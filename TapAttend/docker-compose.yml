services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tapattend
      POSTGRES_PASSWORD: tapattend
      POSTGRES_DB: tapattend
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tapattend"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://tapattend:tapattend@db:5432/tapattend
      JWT_SECRET: change-me-in-production
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
